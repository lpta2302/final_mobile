<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Màu nền (nếu không muốn nền, dùng `@android:color/transparent`) -->
    <stroke
        android:width="2dp"
        android:color="#FF5722" /> <!-- Độ dày và màu của border -->
    <corners android:radius="100dp" /> <!-- Bo góc -->
</shape>
